@model IEnumerable<LibraryManagementSystem_A2.Models.LibraryBranch>

@{
    ViewBag.Title = "Index";
}

<head>
    <style>
        .newbox {
            background: rgba(255,255,255,0.6);
        }

        h2, p {
            text-align: center;
        }
    </style>
</head>
<body background="~/images/book.jpg" style="background-size:cover">
    <div class="newbox">
        <h2>Library Branches</h2>
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Address)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Phone)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Hours)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Address)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Phone)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Hours)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LibraryBranchId }) |
            @Html.ActionLink("Details", "Details", new { id=item.LibraryBranchId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LibraryBranchId })
        </td>
    </tr>
}

</table>
        </div>
</body>